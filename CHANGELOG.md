# Changelog

所有显著变更记录于此。格式参考 Keep a Changelog；版本号语义化。

## [0.10.0] - 2026-08-21

### 新增
- **费用估算**：按模型参考价把 token 换算成人民币（内置 DeepSeek 官方空闲时段价
  `deepseek-v4-flash / -vision-exp 1.5/0.05/4.5`、`deepseek-v4-pro 4.5/0.15/13.5`，
  高峰 ×2，故估算为"至少"值；`deepseek-chat / -reasoner` 旧参考价保留）。
  键支持**前缀匹配**（`deepseek-v4-flash-0731` / `DeepSeek-V4-Flash-0731` 均命中），
  未匹配模型费用显示 `—` 并计入「未计价」；`config.prices` 按模型覆盖
  （`input/cacheRead/cacheWrite/output/reasoning`，缺省 `cacheWrite=input`、`reasoning=output`）
- **官方余额**：新路由 `GET /token-stats/balance`（官方 `GET /user/balance`，
  `DEEPSEEK_API_KEY` 经 credentials 通道解析，服务端 60s 缓存；未配置密钥返回
  `ok:false / no-api-key`，客户端优雅隐藏）
- 设置页「用量统计」：概览次行「估算费用 ¥…」（未计价提示）+「官方余额 ¥…」；
  模型明细表与 7 天 / 30 天逐日表新增「费用」列；热力图 tooltip 与迷你热力图
  title 同步带估算费用；CSV 模型段新增「估算费用(元)」列
- 宠物弹窗 meta 行显示「估算费用 ¥…」；`/usage` 输出估算费用（含未计价提示）
  与官方余额（可选，仅当密钥可解析且请求成功）
- 插件配置表单新增 `prices` 字典（每模型单价），GUI 修改实时生效

### 变更
- summary / history 响应为每模型携带 `cost`（null = 未计价）与全天 `unpriced` 计数，
  旧客户端字段缺失时优雅降级（无 cost 字段 → 不展示费用）
- `/usage` 命令 handler 改为异步（追加官方余额），`buildUsageText` 增加第三个
  参数 `prices`（缺省内置参考价，向后兼容）

### 测试
- 新增 E1-E4（前缀匹配 / 覆盖价 / 未计价 / history 费用）、F1-F3（balance 路由
  mock credentials + mock fetch：成功 / 60s 缓存 / 无密钥降级），33 项全通过
- 新增 `verify-cost-ui.py`：mock 新服务端响应的 Playwright 断言（概览费用与余额、
  模型表 / 7 天表「费用」列、宠物弹窗估算费用、旧服务端无 cost 兼容、无页面错误）

## [0.9.9] - 2026-08-21

### 新增
- `/usage [天数]` 参数：`/usage` 仅今天，`/usage 7` 汇总最近 7 天（区间合计、
  每日趋势条、子代理对账、按提供商/模型明细降序）；非法参数回退今天，上限 366
- 命令注册声明 `input` 占位提示（`[天数，缺省 1=今天，最大 366]`）

### 变更
- `/usage` 文案生成重构为模块级纯函数 `buildUsageText(state, days)`，可独立测试
- 测试新增 A6c（区间汇总）/ A6d（非法参数回退），24 项全通过

## [0.9.8] - 2026-08-21

### 新增
- 会话表子代理行粉底「子」徽标 + 父会话前缀
- 概览卡「今日速率 X / 时」（按当日已过时长折算，仅今天视图）
- 热力图月份标签悬停显示该月计费输入合计

## [0.9.7] - 2026-08-21

### 新增
- 热力图格子点击 → 跳到该天「当日用量」视图（与日期导航打通，平滑滚动定位）
- 会话 ID 点击复制完整值（✓ 反馈 1.5s，剪贴板不可用时优雅降级）

## [0.9.6] - 2026-08-21

### 新增
- `/usage` 输出近 7 天趋势条（`▁▂▃▄▅▆▇█` 八档，零用量日记 `·`）

### 变更
- `/usage` 文案抽取为纯函数 `buildUsageText`（测试覆盖）

## [0.9.5] - 2026-08-21

### 新增
- CSV 导出包含「会话明细」段（子代理/父会话/最后活动时间）
- 深色主题 Playwright 回归（`ui8-dark.py`，结束自动还原主题）

## [0.9.4] - 2026-08-21

### 新增
- 设置页日期导航：‹ › 浏览近 365 天任意一天，历史天标题「当日用量」+「回到今天」
- 头部 ⤓ 导出所选天明细 CSV（UTF-8 BOM）

## [0.9.3] - 2026-08-21

### 新增
- 热力图悬浮卡片（日期星期/档位点/计费输入/输出/请求，事件委托）
- 宠物弹窗内嵌「近 30 周」迷你热力图（模块级缓存，每次页面加载只拉一次）

## [0.9.2] - 2026-08-21

### 新增
- 设置页 GitHub 风格「活跃热力图」：周×星期网格、计费输入 5 档（非零日四分位数）、
  今天描边、月份/星期标尺、懒加载近 12 个月、窄容器横向滚动且自动滚到最右

### 变更
- `/token-stats/history` 天数上限 30 → 366（需重启 dsh web 生效）

## [0.9.1] - 2026-08-21

### 变更
- 宠物：身体径向渐变、晕眩螺旋眼自旋（替代 X 眼）、弹窗标题小史莱姆、空态史莱姆
- 设置页：主指标标签统一中文「计费输入」、近 7 天趋势 sparkline + 较昨日增幅、
  命中率展示去重、刷新按钮旋转、对账行无子代理时省略零项、图表「今天」标签

## [0.9.0] - 2026-08-21

### 新增
- 宠物生命力：情绪 180ms 弹入、待机浮动、眼睛左顾右盼、工作态光标/加载点、
  晕眩双星环绕 + 摇摆、面板情绪行
- 设置页可折叠「最近 30 天」视图（懒加载）

## [0.8.0] - 2026-08-21

### 变更
- 宠物参考 Codex 桌宠（Boba / Desk Otter）设计语言：角色描边、地面阴影、64px、
  hover 探头、双击逗宠物（爱心 + 蹦跳）、完成/收工提示卡片

## [0.7.0] - 2026-08-21

### 新增
- 实时活动：`/token-stats/summary` 附带 activity，宠物「工作中」状态
  （专注盯小电脑 + 脉冲点 + 光环）、完成/收工提示

## [0.6.0] - 2026-08-21

### 新增
- 悬浮用量宠物（`shell.overlay`）：可拖拽史莱姆，情绪 = 工作状态 + 今日用量；
  侧边栏底部显示/隐藏开关

## [0.4.0] - 2026-08-21

### 新增
- 设置页「插件配置」表单（storagePath / keepDays，GUI 修改实时生效）

## [0.3.0] - 2026-08-21

### 新增
- 会话维度统计（`/token-stats/sessions`）+ 顶层/子代理对账（设置页与 /usage）

## [0.3.1] - 2026-08-21

### 变更
- 设置页改为克制数据面板（主数字 + 留白分隔 + 发丝线表格）

## [0.2.0] - 2026-08-21

### 新增
- 四处展示：悬浮宠物 / 侧边栏开关 / 设置页分区 / `/usage` 命令

## [0.1.1] - 2026-08-21

### 修复
- 会话日志为多帧 zstd 容器，旧版只解第一帧导致启动回填漏计约 3/4 用量；
  改为按帧完整解码 + 每次加载重建今日（不漏计、不重复）

## [0.1.0] - 2026-08-21

### 新增
- 首个版本：订阅 `assistant/message` 按自然日聚合 token 用量（提供商/模型），
  原子持久化，`/token-stats/summary|history|sessions` HTTP API，`/usage` 命令
